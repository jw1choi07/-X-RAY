# 약관 X-ray · 작업 기록

세션마다 진행 내용을 여기에 계속 누적. 최신 내용이 아래로 추가됨.

---

## 2026-07-22

- MCP 인증 이슈: Nimble MCP 서버(mcp.nimbleway.com)에서 HTTP 421 (Misdirected Request) 발생 — Claude Code 인증 문제 아니라 서버 측 라우팅 문제로 추정. 미해결 상태로 남겨둠.
- 프로젝트 현황 파악: `web/` 디렉토리가 실제 서비스(Next.js). 최근 커밋은 Clerk 로그인 + `/admin` 라우트 보호(`ef755de`, 7/19).
- **AI Document Builders Challenge 루브릭** 확보 (Discord 공유 이미지) → `AI_Document_Builders_Challenge_루브릭.md`로 저장. 총 100점, 항목: Service Differentiation(10) / Data Architecture & Process(20) / Solution Depth(15) / Service Impact(20) / Effective Use of Upstage(20) / Presentation & Documentation(15).
- 현재 상태 기준 자가진단 점수 추정: **~64/100**. 최대 약점은 Data Architecture & Process(파싱 실패 3건, 크롤링 커버리지 공백)와 Effective Use of Upstage(Solar만 사용, Document Parse/Information Extract/Embed 2 미구현). → `발표_준비_자가진단.md`에 상세 정리.
- 발표 준비용 종합 정리 파일 `발표_준비_자가진단.md` 생성 (프로젝트 현황 + 루브릭 + 자가진단 + 멘토링 브리핑 핵심 요약).
- `web/docs/`에 루브릭·자가진단·작업기록 커밋 후 GitHub(`jw1choi07/-X-RAY`) push. 팀원의 "메뉴 및 추가 기능 구현" 커밋(마이사이트, 사이드메뉴, 유저 설정)과 병합됨.
- 데모데이: 2026-07-25.
- **추가 크롤링**: 목표 서비스 117개 중 33개(62개 문서)만 크롤링된 상태 확인 → 나머지 84개 중 이미 실패 확인된 3개(네이버·페이스북·무신사) 제외 81개 대상으로 배치 크롤러 작성.
  - `pipeline/batch_crawl.py` 신규 작성: 홈페이지 HTML에서 "이용약관/개인정보처리방침" 텍스트가 붙은 링크를 자동 탐색(BeautifulSoup) → `crawl.py`의 `fetch_document`로 크롤링, `ThreadPoolExecutor`로 8개 동시 처리.
  - 결과: 81개 중 성공 11개 서비스(16개 문서) — 구글, 당근페이, 레진코믹스, 메가스터디, 밀리의서재, 옥션, 원티드, 줌(ZUM), 크래프톤, 페이코, 헬로마켓. 정크(글자수 미달/영문 위주) 9건, 중복(terms==privacy 동일 페이지) 2건은 제거.
  - 실패 원인: robots_blocked 12건(X, 뱅크샐러드, 지니뮤직, 직방, 카카오맵, 카카오뱅크, 카카오톡), no_links_found 40개 서비스(JS 렌더링 푸터라 정적 파싱으로 링크 탐색 실패 — Playwright 렌더링 탐색으로 개선 여지 있음), homepage_failed 9건(403/DNS/타임아웃 — 쿠팡, Claude, 삼성 계열 등).
  - **데이터셋 규모: 62 → 78개 문서**로 증가. `data/texts/`(root)와 `web/data/texts/`(배포용) 양쪽에 반영, `web/` 리포에 커밋 완료(`8865e36`). 결과 리포트는 `data/batch_crawl_report.json`, `data/batch_crawl_summary.txt`에 저장 (git 미추적, 로컬 전용).
  - 남은 40개(no_links_found)를 더 확보하려면 홈페이지 렌더링 폴백 또는 개별 URL 수동 지정이 필요.
- **2차 재시도 (no_links_found 40개 대상)**: `batch_crawl.py`에 홈페이지 링크 탐색용 Playwright 렌더링 폴백 추가(정적 HTML에서 못 찾으면 렌더링 후 재탐색). 4개 동시 처리로 재실행.
  - 결과: 5개 서비스 신규 확보 — 다방, 부킹닷컴, 카카오페이지(각 이용약관+개인정보처리방침), 테무(개인정보처리방침만), 트리플(이용약관만). 영문 위주(Gemini, 라인 개인정보처리방침, 스레드) 및 빈 페이지(트리플 개인정보처리방침 27자)는 정크로 제거.
  - **데이터셋 규모: 78 → 86개 문서**로 증가.
  - 여전히 남은 케이스: robots_blocked 4건 추가 확인(KB스타뱅킹, MS365, 다음, 번개장터), 여기어때(404 — URL 오탐), 렌더링도 타임아웃/연결오류난 9곳(네이버밴드, 네이트, 듀오링고, 빗썸, 사람인, 에어비앤비, 인프런, 타다, 티빙, 퍼플렉시티), 그 외 아직 안 잡힌 곳(그린카, 땡겨요, 로블록스, 아고다, 알리익스프레스, 업비트, 인스타그램, 잡코리아, 줌Zoom, 캐치테이블, 캔바, 코레일톡, 코파일럿, 키움증권, 텔레그램, 토스증권, 티몬) — 이 정도는 개별 URL을 직접 지정해서 시도하는 수동 방식이 필요해 보임.
- **수집 대상을 개인정보처리방침으로 일원화**: 서비스 핵심 가치("이 서비스가 내 정보로 뭘 하는지")가 개인정보처리방침 위주 위험 카테고리(제3자 제공, 추적기술, 마케팅활용, 위치정보)와 맞닿아있어 이용약관보다 우선순위 높다고 판단. 기존 이용약관 39개 파일 전부 삭제(메가스터디·밀리의서재·트리플은 개인정보처리방침이 없어서 통째로 데이터셋에서 빠짐). **데이터셋 규모: 86 → 47개(전부 개인정보처리방침)**.
- robots.txt로 막힌 서비스(카카오톡 등)는 자동 크롤링 대신 **팀원이 브라우저에서 PDF로 저장해서 전달 → Claude가 읽어서 텍스트 추출·저장**하는 수동 보완 방식으로 전환 예정. 남은 서비스 홈페이지 링크 목록을 채팅에 정리해서 전달함.
- **정확한 URL 검색 후 직접 크롤링 (3차)**: fork 에이전트로 WebSearch를 이용해 남은 서비스들의 정확한 개인정보처리방침 URL을 검색 → `pipeline/direct_crawl.py` 신규 작성(URL별로 robots.txt 확인 후 `fetch_document`로 직접 수집, 6개 동시 처리).
  - 결과: 48개 URL 중 18개 저장 성공 → 품질 재검수(정책 관련 키워드 밀도 확인)에서 2개(여기어때·키움증권 — 실제로는 사이트 내비게이션 텍스트만 긁힘) 추가 제거. **최종 16개 신규 확보**: Gemini, MS365, 넷마블, 미래에셋증권, 밀리의서재(중복 대체), 사람인, 아고다, 알리익스프레스, 업비트, 엔씨소프트, 인프런, 잡코리아, 줌(Zoom), 코레일톡, 코파일럿, 쿠팡.
  - robots_blocked 14건(KB스타뱅킹, 네이트, 다음, 디즈니플러스, 무신사, 뱅크샐러드, 번개장터, 웨이브, 지니뮤직, 직방, 카카오뱅크, 카카오톡, 캐치테이블, 페이스북), 접속 오류 8건(Claude, 굿닥, 그린카, 빗썸, 위메프, 인스타그램, 크림, 티몬), 정크 9건(넥슨, 로블록스, 메가스터디, 스팀, 인바디, 쿠팡플레이, 타다, 트리플, 퍼플렉시티)은 여전히 수동 처리 필요.
  - **데이터셋 규모: 47 → 63개(전부 개인정보처리방침)**. `web/` 리포에 커밋 완료(`5b0be82`).
- **목표 리스트 250개로 확장**: 기존 117개 목록에 새 카테고리(보험·증권·렌터카·교육·데이팅·생산성툴 등) 112개를 추가한 `pipeline/batch_crawl2.py` 작성 → 동일한 footer-link 탐색 파이프라인으로 크롤링.
  - 결과: 70개 서비스에서 문서 확보 시도 성공 → 이용약관 제거 + 품질검수(길이/한국어 비율) 후 **32개 신규 개인정보처리방침 확보**. 영문 위주(라이엇게임즈, 미드저니, 캐릭터AI, 범블, 틴더, 트립닷컴 등 글로벌 서비스)와 정크(대신증권, 아프리카TV, 화해 등)는 15개 제거.
  - **데이터셋 규모: 63 → 95개**. `web/` 리포 커밋(`703420c`) 및 push 완료.

## 2026-07-25 (멘토링 미팅 당일)

- **Upstage 멘토 피드백 정리** (`멘토링_피드백_정리.md`): 브리핑에서 준비한 질문 9개에 대한 실제 답변을 매칭해서 정리.
  - Q1(Groundedness Check 폐지): 공식 문서에도 잘 안 보임 — Groundedness Checking API(`console.upstage.ai/api/groundedness-checking`)를 직접 모델처럼 써볼 것 제안.
  - Q2(Structured Output vs 프롬프트 JSON): Structured Output 반드시 사용 권장 — 지금 파싱 실패 3건 해결 가능성.
  - Q3(Document Parse): 표/레이아웃 복잡한 문서에 특히 유용, 사용 권장 (`console.upstage.ai/api/docs/for-agents/raw`).
  - Q4(Information Extract/청크): AI로 문서를 잘라서(청크) 넣는 방식 권장, 크롤링도 주기 배정해서 분산.
  - Q5/Q6(RAG 성능·매칭 타당성 낮음): 텍스트 유사도만으론 부족 — **벡터DB에 넣을 때부터 메타데이터를 설계해서 넣고, 검색 시 메타데이터 우선 필터링** 하는 구조 권장. 에이전틱 루프(검색→충분성 판단→재검색/진행), Query Rewriting/Expansion도 고려.
  - 추가: Solar Pro 2 vs Pro 3 시간 효율성 벤치마크해서 발표에 포함 권장. 현재 Google Sheets 기반 데모 → 향후 DB 전환 계획을 발표에서 설명하면 좋다는 조언.
  - **우선순위**: ①Structured Output 전환 ②Groundedness Checking API 교체 실험 ③메타데이터 기반 검색 설계를 로드맵으로 발표자료에 포함 ④Solar Pro 3 벤치마크 슬라이드 ⑤시간 남으면 Document Parse/청크 분할.
- **전문가 자문 보고서 작성** (`전문가_자문_보고서.md`): 팀에서 미리 준비해둔 HWPX 양식(전문가/팀명/자문일시/요청내용/자문내용) 구조를 그대로 따라 멘토링 내용을 채워 넣음. 자문 일시 2026-07-17(금) 10:00~10:30(30분), 전문가 최선웅, 팀명 업스타트. HWPX 원본 zip을 파이썬으로 직접 파싱·수정해서 실제 양식 파일(`전문가_자문_보고서.hwpx`)도 채워 넣음 (XML 유효성 검증 완료). 최종적으로는 팀원이 텍스트를 직접 복사해서 넣는 방식으로 마무리.
- **150개 목표 크롤링 (5~6차)**: 사용자가 "150개까지 성공할 때까지 돌려"라고 요청 → 신규 카테고리(편의점·프랜차이즈·뷰티·자동차·코워킹 등) 총 242개(`batch_crawl3.py` 146개 + `batch_crawl4.py` 151개, 중복 포함) 추가 시도.
  - 3차: 146개 시도 → 정크/중복 필터링 후 20개 신규 확보 (115개로 증가)
  - 4차: 151개 시도 → 39개 성공 판정 중 CU·고캠핑·롯데시네마는 수십만~118만자짜리 메뉴/이력 통짜라 별도 제외, 최종 38개 확보
  - **데이터셋 규모: 115 → 153개 — 150개 목표 달성**. `web/` 리포 커밋(`391c78d`, `13451ac`).
- **UI 카테고리 브라우저 추가**: 153개 중 147개가 실제 프리셋 목록(웹 API)에 뜨는 것 확인(나머지 6개는 영문/너무 짧음으로 앱 자체 필터에 걸림 — 4개는 원래부터 있던 파일, 2개(멜론·클래스101)는 재크롤링해도 JS 지연로딩 때문에 여전히 짧게 잡힘, 필터로 자동 숨김 처리라 문제 없음).
  - 검색 전용이던 UI에 "카테고리로 찾아보기" 토글 추가 — `src/lib/site-categories.ts`(153개 서비스 → 19개 카테고리 매핑) 신규 작성, `presets.ts`에 category/siteName 필드 추가, `search-section.tsx`에 카테고리 pill + 사이트 목록 UI 구현.
  - **버그 발견 및 수정**: 홈 화면 플로팅 카드(`featured-sites.ts`)가 예전에 삭제한 `_이용약관.txt` 파일을 여전히 참조하고 있어 클릭 시 깨지는 상태였음 → `_개인정보처리방침.txt`로 전부 교체.
  - dev 서버 + 브라우저로 카테고리 클릭→사이트 선택→실제 분석까지 전체 플로우 검증 완료(요기요로 테스트). `web/` 커밋(`7248a68`) 및 push 완료.
